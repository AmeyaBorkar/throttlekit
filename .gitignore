node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.*
.idea/
.vscode/
*.tsbuildinfo
.vitest/
.tsup/
.stryker-tmp/
reports/
tmp/

# TLA+ / TLC model-checker artifacts (the spec is tracked; the jar and scratch are not)
tla2tools.jar
states/
*.st

# Claude Code session state — runtime-only, never committed.
.claude/

# Strategic / unpublished research kept LOCAL (0.13.0, #206): the roadmap + paper drafts.
# The DESIGN docs, verification gates, and eval data (.ts/.json/.csv/.svg) stay tracked as PUBLIC
# evidence — only these prose documents are private. They stay on disk, just out of the repo.
research/bigger-bets/PLAN.md
research/paper/
research/website/
research/gale/*.md
research/cost-uncertainty/*.md
