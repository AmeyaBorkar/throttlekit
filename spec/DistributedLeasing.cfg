\* TLC model for ThrottleKit distributed leasing overshoot.
\* Bound under this model: Limit + |Nodes| * (Batch - 1) = 4 + 2*1 = 6.
\* This committed config PASSES (no invariant violation).
\*
\* To reproduce the tightness counterexample, add `OvershootTight` to
\* INVARIANTS below and re-run; TLC reports a violation reaching
\* admitted = 6. See spec/README.md for the captured trace.

SPECIFICATION Spec

CONSTANTS
    Nodes = {n1, n2}
    Limit = 4
    Batch = 2

INVARIANTS
    TypeOK
    Overshoot
